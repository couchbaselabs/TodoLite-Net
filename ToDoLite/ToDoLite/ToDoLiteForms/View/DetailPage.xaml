<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ToDoLiteForms.View.DetailPage">
  <StackLayout>
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <Button Text="Share" Grid.Column="1" />
    </Grid>
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Image Source="{Binding AddTaskImageSource}" />
      <Entry Placeholder="Add Task" HorizontalOptions="FillAndExpand" Grid.Column="1" Completed="AddTaskCompleted"
             Text="{Binding AddTaskText, Mode=TwoWay}"/>
    </Grid>
    <ListView ItemsSource="{Binding TaskItemsSource}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ImageCell Text="{Binding Text}" ImageSource="{Binding ImageSource}"/>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </StackLayout>
</ContentPage>